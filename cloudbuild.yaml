options:
  logging: CLOUD_LOGGING_ONLY

steps:
  # 1. Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t',
      'europe-west4-docker.pkg.dev/trybae-457418/ai-repo/ai-service:latest',
      '.'
    ]

  # 2. Push Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push',
      'europe-west4-docker.pkg.dev/trybae-457418/ai-repo/ai-service:latest'
    ]

  # 3. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        gcloud run deploy ai-service \
          --image europe-west4-docker.pkg.dev/trybae-457418/ai-repo/ai-service:latest \
          --platform managed \
          --region europe-west4 \
          --allow-unauthenticated

images:
  - 'europe-west4-docker.pkg.dev/trybae-457418/ai-repo/ai-service:latest'
